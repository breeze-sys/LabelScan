# Data Fixtures

This directory stores the CIFAR-10 binary batches used by the built-in demo
workflow. The Go audit engine reads these files directly when running the local
target/shadow model example.

CIFAR-10 is a third-party dataset and is not relicensed by this project. Users
should follow the dataset provider's original terms and citation requirements.
