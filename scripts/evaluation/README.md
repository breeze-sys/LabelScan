# Evaluation Utilities

This directory contains optional helper scripts for reproducing evaluation
metadata and offline analysis.

These scripts are not required for the Docker quick start or the Go audit
engine. Run them from the repository root when regenerating local experiment
artifacts.

```bash
python scripts/evaluation/export_members.py
python scripts/evaluation/eval.py
```
